# @asciip/web

Next.js 15 frontend. Phase 1 ships a placeholder `package.json` + stub page so the pnpm workspace resolves and Docker can reference it. Phases 6 and 7 deliver the full design system and every page per `ui_spec.md`.
