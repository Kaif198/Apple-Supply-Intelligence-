# asciip-api

FastAPI backend. Phase 1 ships a minimal health-only app so `make up` succeeds end-to-end; Phase 5 delivers every endpoint in `design.md` §Endpoints with middleware, services, OpenAPI docs, and SSE streaming.
