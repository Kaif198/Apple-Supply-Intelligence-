# asciip-data-pipeline

Layer 2/3 of ASCIIP — external source adapters, DuckDB feature store, supplier PDF extraction.

Phase 1 ships only the `bootstrap` entrypoint (directory tree + key validation). Phase 2 adds every source adapter; Phase 3 adds the feature store.
